SmartPay CRM v3.10.96 — Comment History Context & Order

Исправлено:
- при добавлении комментария в Истории теперь явно указаны мерчант и текст комментария;
- отдельные действия «Добавлен комментарий» и «Редактирование комментариев»;
- обычное редактирование мерчанта тоже содержит строку «Мерчант: ...»;
- история комментариев в карточке сортируется от новых к старым по реальной дате/времени;
- многострочные комментарии больше не разбиваются на отдельные «даты»;
- в карточке сверху показывается последний комментарий, а полная история — ниже;
- окно управления комментариями показывает аккуратную хронологию.

Заменить оба файла: index.html и Code.gs.
После замены Code.gs обновить deployment Apps Script и выполнить Ctrl+F5.
