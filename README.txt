SmartPay CRM v3.10.75 — быстрая фоновая синхронизация статусов задач

Что исправлено:
- при переносе задачи между колонками Kanban больше не появляется полноэкранная загрузка;
- статус сохраняется одним запросом напрямую в Google Sheets;
- повторная полная загрузка всех данных после каждого перемещения удалена;
- интерфейс меняется сразу, а при ошибке задача автоматически возвращается в прежнюю колонку;
- кнопки «В работу» и «Выполнить» также работают без блокировки всей страницы.

Заменить только index.html.
Code.gs из v3.10.74 менять не нужно.
После замены выполнить Ctrl + F5.
